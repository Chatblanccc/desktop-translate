# Eligible blind-evaluation fixtures

Only reviewed public-dataset or self-authored synthetic JSONL belongs here.
Every record must validate against
`../../schemas/blind-evaluation-input.schema.json`.

Do not place user selection history, clipboard content, private corpora,
personal data, credentials, or ad-hoc/free-form source text in this directory.
Large or locally licensed evaluation sets belong in the ignored
`artifacts/phase7/offline-poc/blind-eval-input/` root instead.
